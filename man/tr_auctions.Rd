% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/securities.R
\name{tr_auctions}
\alias{tr_auctions}
\alias{tr_announcements}
\alias{tr_upcoming}
\title{US Treasury marketable securities and auction results}
\source{
\url{https://www.treasurydirect.gov/webapis/webapisecurities/}
}
\usage{
tr_auctions(type = NULL, days = NULL)

tr_announcements(type = NULL, days = NULL)

tr_upcoming(type = NULL)
}
\arguments{
\item{type}{(\code{NULL} | \code{character(1)})\cr
Security type to filter by, one of \code{"Bill"}, \code{"Note"}, \code{"Bond"}, \code{"CMB"}, \code{"TIPS"}, or
\code{"FRN"}. If \code{NULL}, all types are returned. Default \code{NULL}.}

\item{days}{(\code{NULL} | \code{integer(1)})\cr
Restrict the results to securities from the last \code{days} days. If \code{NULL}, the most recent
securities are returned. Default \code{NULL}.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} containing the securities or \code{NULL} when no entries were
found.
}
\description{
Retrieve data on US Treasury marketable securities from the TreasuryDirect API.
\code{tr_auctions()} returns auction results for securities that have already been auctioned,
\code{tr_announcements()} returns securities whose auctions have been announced but not yet held, and
\code{tr_upcoming()} returns the upcoming auction schedule.
}
\details{
These functions return every field provided by the API (around 120 columns), with names
converted to snake case, date columns parsed to \link{Date} and numeric columns parsed to numeric.
Fields that do not apply to a given security type are returned as \code{NA}. The API returns at most
the 250 most recent matching securities.
}
\examples{
\donttest{
# auction results for the most recently auctioned notes
tr_auctions("Note")
# announced bill auctions
tr_announcements("Bill")
# upcoming auction schedule
tr_upcoming()
}
}
\concept{auction}
